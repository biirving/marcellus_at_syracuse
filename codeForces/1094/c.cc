
// given array a with odd length n consisting of positive ints
// partition a into several sub arrays with odd legnths and same median (same center value)


// I can do this
// just takes some practice

//https://codeforces.com/contest/2222/problem/c
#include <stdio.h>
#include <stdbool.h>
#include <vector>
#include <unordered_set>
#include <algorithm>
using namespace std;

int main() {
    // First scan in number of test cases
    int t;
    scanf("%d", &t);
    int m, n;

    int x[100000];

    int odd_length;
    int even_length;

    // iterate through the test cases
    for (int test_index = 0; test_index < t; test_index++) {

    }
}